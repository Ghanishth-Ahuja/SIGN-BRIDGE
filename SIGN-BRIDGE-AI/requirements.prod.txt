# Production requirements - MINIMAL
# Only what's absolutely required for inference (not training)

# --- Core Web & Auth ---
Flask==3.0.3
flask-cors==4.0.0
flask-jwt-extended==4.6.0
Werkzeug==3.0.3
gunicorn==23.0.0
eventlet==0.35.2
python-socketio==5.11.0
bidict==0.23.1

# --- Authentication ---
bcrypt==4.1.3
PyJWT==2.8.0
google-auth==2.29.0

# --- ML Inference (TFLite only, NO TensorFlow) ---
tflite-runtime==2.14.0
numpy==1.26.4

# --- Computer Vision ---
# Note: mediapipe is installed separately in Dockerfile to control deps
opencv-python-headless==4.9.0.80
pillow==10.3.0

# --- Utilities ---
requests==2.31.0
urllib3==2.2.1
certifi==2024.2.2
charset-normalizer==3.3.2
idna==3.7
